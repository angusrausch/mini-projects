vrrp_instance VI_1 {
    state BACKUP
    interface eth0 # Should be eth0 on Docker
    virtual_router_id 51
    priority 90
    advert_int 1
    virtual_ipaddress {
        # your ip here + subnet mask
    }
}
